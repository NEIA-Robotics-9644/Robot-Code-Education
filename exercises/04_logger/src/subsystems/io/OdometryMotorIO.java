package subsystems.io;

public interface OdometryMotorIO {
    /* TODOS:
        1. Add method to execute a list of "MovementStep"s:
            - executeStep(List<MovementStep> steps)
        2. Ensure all implementations handle safety and invalid inputs.
    */
}
