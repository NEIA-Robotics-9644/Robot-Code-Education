/*
    This is where subsystems are created and connected.

    TODOS:
      1. Add a field for the OdometrySubsystem
      2. Add a constructor that:
           - prints when RobotContainer is created
           - creates a OdometryMotorIOSim object
           - passes them into a new OdometrySubsystem
      3. Add a getter for the OdometrySubsystem so Robot can access it
*/

import subsystems.OdometrySubsystem;
import subsystems.io.OdometryMotorIO;
import subsystems.io.OdometryMotorIOSim;

public class RobotContainer {

    // TODO 1: Add a OdometrySubsystem field
    

    // TODO 2: Add a constructor that wires all subsystems together
    public RobotContainer() {

    }

    // TODO 3: Add a getter for OdometrySubsystem
}
